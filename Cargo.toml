[package]
name = "yassy"
version = "0.1.0"
authors = ["stefan"]

[dependencies]
libc = "*"
num = "*"

[lib]
name = "yassy"
crate-type = ["dylib"] # ?
test = false # disable unit tests for this target unit. annoying output

[dev-dependencies] # for testing/documentation
# GSL = "*"
gnuplot = "*"
getopts = "=0.2.14" # gnuplot dependency
